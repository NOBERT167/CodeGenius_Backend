public class {{ model_name }}
{
    {% for param in parameters %}{% if param.name.lower() != 'docno' and param.name.lower() != 'userid' %}
    public {{ param.type }} {{ param.csharp_name }} { get; set; }
    {% endif %}{% endfor %}
}